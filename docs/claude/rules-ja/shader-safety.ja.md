# HLSL Rules — 日本語ミラー

register binding、constant buffer配置、input semantics、texture/sampler、matrix convention、entry pointを挙動上重要として扱います。機械的整理でlighting値、dissolve threshold、shadow判定などを変えません。意図的な見た目変更はBefore/After画像で検証します。
